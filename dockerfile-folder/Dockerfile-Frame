#베이스 이미지를 명시해 준다.
#<이미지 이름>:<태그> 형식으로 작성 
#ex)ubuntu:14.04 

FROM baseImage

#도커 이미지가 생성되기 전에 수행할 쉘 명령어 
#주로 추가적으로 필요한 파일을 다운로드 받는다.
RUN command

#컨테이너가 시작되었을 때 실행할 
#실행 파일 또는 쉘 명령어 
#해당 명령어는 DockerFile내 1회만 사용할 수 있다.
CMD ["executable"] 